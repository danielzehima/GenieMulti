# URL du webhook externe (n8n, Zapier, Make...) qui reçoit les leads
# et déclenche les relances automatiques (email + WhatsApp).
# Laisser vide en développement : les leads seront simplement loggués côté serveur.
LEADS_WEBHOOK_URL=
